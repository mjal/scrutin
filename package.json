{
  "name": "scrutin-workspace",
  "workspaces": {
    "packages": [
      "app",
      "server"
    ]
  },
  "scripts": {
    "re:start": "watchexec --project-origin . -c -w myproject/src -w helpers/src -i lib -e res,resi 'rescript build -with-deps'"
  },
  "dependencies": {
    "scrutin": "file:app"
  }
}
